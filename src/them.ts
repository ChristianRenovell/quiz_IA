import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
      primary: {
        main: '#ffffff',
      },
      secondary: {
        main: '#424141', 
      },
      background: {
        default: '#424141', 
      },
      text: {
        primary: '#241a00', 
      },
    }
  });